name: repo-hygiene
on: [push, pull_request]

permissions:
  contents: read

concurrency:
  group: repo-hygiene-${{ github.ref }}
  cancel-in-progress: true

jobs:
  hygiene:
    uses: asafr-dev/reusable-workflows/.github/workflows/reusable-repo-hygiene.yml@v1
    with:
      strict: true
