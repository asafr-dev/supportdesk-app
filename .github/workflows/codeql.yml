name: codeql

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "21 3 * * 2"
  workflow_dispatch:

jobs:
  analyze:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: asafr-dev/reusable-workflows/.github/workflows/reusable-codeql.yml@v1
    with:
      languages: "javascript-typescript"
