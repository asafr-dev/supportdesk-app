# Future ideas

Potential next steps (kept out of this demo for reviewability):

- Add a minimal **email notification** on status changes.
- Add **attachments** (S3/local) + virus scan.
- Add **more test coverage** around API routes (auth + status transitions).
- Add **observability**: request ids in UI, structured logs shipped to a collector.
